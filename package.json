{
  "name": "aegischat",
  "version": "1.0.0",
  "description": "Discord anti-scam bot using hybrid detection (heuristics + OpenRouter AI)",
  "main": "src/index.js",
  "scripts": {
    "start": "bun run src/index.js",
    "dev": "bun run --watch src/index.js",
    "seed": "bun run scripts/seedScamImages.js",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "axios": "^1.18.1",
    "discord.js": "^14.27.0",
    "dotenv": "^17.4.2",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "prettier": "^3.9.5"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
